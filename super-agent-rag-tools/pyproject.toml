[project]
name = "super-agent-rag-tools"
version = "0.1.0"
description = "Python RAG tool service for super-agent"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0"
]

[tool.uvicorn]
factory = false
